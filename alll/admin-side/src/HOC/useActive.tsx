import React from 'react'
import { useSelector } from 'react-redux';
// import { getCode } from 'redux/admin/auth-manage/login/login.reducer';

export default function useActive() {
    // const codeActive = useSelector(getCode)
    // return codeActive == '400';
}
